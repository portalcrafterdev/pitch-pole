<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Play Games signs in over the network, so this is needed in every build
         and not only in debug. The debug manifest declares it too, for the
         Flutter tool's own connection, which is exactly why its absence here
         is easy to miss: sign in works all through development and then fails
         in the one build that ships. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="Pitchpole"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:appCategory="game">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Google Play Games. Read at app start by the Play Games SDK, so it
             has to be here even before the id in game_ids.xml is a real one. -->
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="@string/games_app_id" />
        <!-- AdMob, likewise: missing here means a crash on launch, not a
             missing ad. See ad_ids.xml. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id" />
        <!-- Which of Android's Game Mode interventions this game accepts.
             android:appCategory="game" above is what makes the system treat it
             as a game at all; this says what it may do about it. See the
             reasoning in res/xml/game_mode_config.xml. -->
        <meta-data
            android:name="android.game_mode_config"
            android:resource="@xml/game_mode_config" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
